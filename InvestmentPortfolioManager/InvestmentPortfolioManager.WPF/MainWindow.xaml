<Window x:Class="InvestmentPortfolioManager.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:InvestmentPortfolioManager.WPF.MVVM"
        xmlns:views="clr-namespace:InvestmentPortfolioManager.WPF.Views"
        mc:Ignorable="d"
        Title="Investment Portfolio Manager" Height="650" Width="1300"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:PortfolioViewModel}">
            <views:PortfolioView />
        </DataTemplate>

        <Style x:Key="MenuButtonTheme" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#BDC3C7"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" Padding="15,8" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#34495E"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Background" Value="#1ABC9C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="true">
                    <Setter Property="ToolTip"
                            Value="{Binding RelativeSource={x:Static RelativeSource.Self},
                            Path=(Validation.Errors)[0].ErrorContent}"/>
                    <Setter Property="BorderBrush" Value="#E74C3C"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Portfolio Manager" 
                               Foreground="White" FontSize="20" FontWeight="Bold" 
                               VerticalAlignment="Center" Margin="0,0,30,0"/>

                    <RadioButton Content="Dashboard" 
                                 Command="{Binding SwitchViewCommand}" CommandParameter="Dashboard"
                                 Style="{StaticResource MenuButtonTheme}" IsChecked="True"/>

                    <RadioButton Content="Assets" 
                                 Command="{Binding SwitchViewCommand}" CommandParameter="Portfolio"
                                 Style="{StaticResource MenuButtonTheme}" Margin="0,0,20,0"/>

                    <Rectangle Width="1" Fill="#34495E" Height="30" Margin="0,0,20,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">

                    <TextBlock Text="Select:" 
                               Foreground="#BDC3C7" 
                               VerticalAlignment="Center" 
                               Margin="0,0,5,0"/>

                    <ComboBox ItemsSource="{Binding AllPortfolios}"
                              SelectedItem="{Binding SelectedPortfolio}"
                              DisplayMemberPath="Name"
                              Width="140" Height="28"
                              VerticalContentAlignment="Center"
                              Padding="5,0"
                              Margin="0,0,15,0"/>

                    <TextBlock Text="Name:" 
                               Foreground="#BDC3C7" 
                               VerticalAlignment="Center" 
                               Margin="0,0,5,0"/>

                    <TextBox Text="{Binding SelectedPortfolio.Name, UpdateSourceTrigger=PropertyChanged}"
                             Width="110" Height="28"
                             VerticalContentAlignment="Center"
                             Padding="5,0"
                             Margin="0,0,15,0"/>

                    <TextBlock Text="Owner:" 
                               Foreground="#BDC3C7" 
                               VerticalAlignment="Center" 
                               Margin="0,0,5,0"/>

                    <TextBox Text="{Binding SelectedPortfolio.Owner, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}"
                             ToolTip="Format: Firstname Lastname (e.g. John Doe)"
                             Width="110" Height="28"
                             VerticalContentAlignment="Center"
                             Padding="5,0"
                             Margin="0,0,15,0"/>

                    <Button Command="{Binding CreatePortfolioCommand}" ToolTip="New Portfolio"
                            Content="+" FontSize="16" FontWeight="Bold"
                            Width="30" Height="28" Margin="0,0,5,0"
                            Background="#3498DB" Foreground="White" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Command="{Binding ClonePortfolioCommand}" ToolTip="Clone Portfolio"
                            Content="📄" FontSize="14"
                            Width="30" Height="28" Margin="0,0,5,0"
                            Background="#9B59B6" Foreground="White" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Command="{Binding DeletePortfolioCommand}" ToolTip="Delete Portfolio"
                            Content="🗑" FontSize="14"
                            Width="30" Height="28" Margin="0,0,5,0"
                            Background="#E74C3C" Foreground="White" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">

                    <Button Content="{Binding SimulationButtonText}" 
                            Command="{Binding ToggleSimulationCommand}"
                            Width="110" Height="28"
                            Background="{Binding SimulationButtonColor}" 
                            Foreground="White" FontWeight="Bold" BorderThickness="0"
                            Margin="0,0,10,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Content="Save" 
                            Command="{Binding SaveCommand}"
                            Width="70" Height="28"
                            Background="#27AE60" Foreground="White" FontWeight="Bold" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

            </Grid>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding CurrentView}" Margin="0"/>
    </Grid>
</Window>