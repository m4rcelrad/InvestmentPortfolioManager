<Window x:Class="InvestmentPortfolioManager.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:InvestmentPortfolioManager.WPF.MVVM"
        xmlns:views="clr-namespace:InvestmentPortfolioManager.WPF.Views"
        mc:Ignorable="d"
        Title="Investment Portfolio Manager" Height="650" Width="1050"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:PortfolioViewModel}">
            <views:PortfolioView />
        </DataTemplate>

        <Style x:Key="MenuButtonTheme" TargetType="RadioButton">
            <Setter Property="Foreground" Value="#BDC3C7"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" Padding="15,8" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Background" Value="#34495E"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Background" Value="#1ABC9C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">

                    <TextBlock Text="Investment Manager" 
                               Foreground="White" FontSize="20" FontWeight="Bold" 
                               VerticalAlignment="Center" Margin="0,0,30,0"/>

                    <RadioButton Content="Dashboard" 
                                 Command="{Binding SwitchViewCommand}" CommandParameter="Dashboard"
                                 Style="{StaticResource MenuButtonTheme}" IsChecked="True"/>

                    <RadioButton Content="Assets" 
                                 Command="{Binding SwitchViewCommand}" CommandParameter="Portfolio"
                                 Style="{StaticResource MenuButtonTheme}" Margin="0,0,20,0"/>

                    <Rectangle Width="1" Fill="#34495E" Height="30" Margin="0,0,20,0"/>

                    <TextBlock Text="Portfel:" 
                               Foreground="#BDC3C7" 
                               VerticalAlignment="Center" 
                               Margin="0,0,10,0"/>

                    <ComboBox ItemsSource="{Binding AllPortfolios}"
                              SelectedItem="{Binding SelectedPortfolio}"
                              DisplayMemberPath="Name"
                              Width="160" Height="28"
                              VerticalContentAlignment="Center"
                              Padding="5,0"
                              Margin="0,0,10,0"/>

                    <Button Content="{Binding SimulationButtonText}" 
                            Command="{Binding ToggleSimulationCommand}"
                            Width="110" Height="28"
                            Background="{Binding SimulationButtonColor}" 
                            Foreground="White" FontWeight="Bold" BorderThickness="0"
                            Margin="0,0,10,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    <Button Content="Save" 
                            Command="{Binding SaveCommand}"
                            Width="105" Height="28"
                            Background="#27AE60" Foreground="White" FontWeight="Bold" BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl Grid.Row="1" Content="{Binding CurrentView}" Margin="0"/>
    </Grid>
</Window>